import React from "react";

const VehicleRequiredPrompt = () => {
  return (
    <span id="make-error" className="invalid normal-case">
      This field is required.
    </span>
  );
};

export default VehicleRequiredPrompt;
